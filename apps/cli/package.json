{
  "name": "@agent-passport/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "agent-passport": "./dist/main.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "skills"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && rm -rf dist/web && cp -R ../web/dist dist/web",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agent-passport/api": "workspace:*",
    "@agent-passport/domain": "workspace:*",
    "@agent-passport/intelligence": "workspace:*",
    "@napi-rs/keyring": "2.1.0",
    "@secretlint/core": "13.0.5",
    "@secretlint/secretlint-rule-preset-recommend": "13.0.5",
    "jose": "6.2.12",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@agent-passport/fixtures": "workspace:*",
    "@agent-passport/web": "workspace:*"
  }
}
