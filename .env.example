PASSPORT_PUBLISH_TOKEN=replace-with-a-local-poc-token
PASSPORT_READ_TOKEN=replace-with-a-different-local-poc-token

# Optional. Keep this server-side; the deterministic workflow works without it.
TYPESAFE_API_KEY=replace-with-a-typesafe-key
